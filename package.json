{
  "name": "id_card_manager",
  "version": "1.0.0",
  "description": "## Root directory ID Card Manager Project",
  "main": "index.js",
  "scripts": {
    "server": "cd server && npm run start:dev",
    "client": "cd client && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently --names \"server,client\" \"npm run server \" \"npm run client\" "
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@neutralinojs/neu": "^9.6.0",
    "concurrently": "^8.0.1"
  }
}
